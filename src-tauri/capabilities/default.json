{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main IsoPlan Lead Desk window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

